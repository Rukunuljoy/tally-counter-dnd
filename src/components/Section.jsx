/* eslint-disable react/prop-types */


const Section = ({
    status
    tasks
    setTasks
    shifts
    drag
    Closed
}) => {
    return (
        <div>
            <h2>{status}</h2>List
        </div>
    );
};

export default Section;